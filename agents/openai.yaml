interface:
  display_name: "Generate Brand VI"
  short_description: "Scope, compile, produce, and verify modular Brand VI"
  default_prompt: "Use $generate-brand-vi to define item-level scope, compile approval-gated production DAGs, integrate approved assets, and verify the complete Brand VI."
